# 运行场景

完整说明已移入工具目录，随工具一起打包，部署后也可以离线阅读：

**[运行场景：什么时候检查，什么时候触发](../codex-quota-keeper/docs/scenarios.md)**

包含 12 类场景，以具体日期、时间和额度数值说明：

- 不开启 AutoAnchor，以及开启后的各种配置组合。
- 每日 09:00 前已使用／未使用、空闲确认、迟到与补跑。
- primary / secondary 正常到期、提前重置、上游统一重置。
- 多个触发器合并、最小间隔、每日上限和立即触发。
- 断网、429、认证异常、关机、多机接管与进程崩溃。
- 应用配置、升级、只读查询与日志含义。

本文保留原链接入口；场景正文只维护工具目录中的一份。
